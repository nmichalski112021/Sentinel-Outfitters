# Product path URLs

Canonical product URLs are `/products/{id}` for every SKU (current and future).

Legacy `product.html?id={id}` 301s to the path URL.

After deploy, re-submit sitemap and request indexing for each `/products/...` URL in Search Console.
